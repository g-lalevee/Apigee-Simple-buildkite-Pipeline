env: 
  API_VERSION: "googleapi"  
  # API_VERSION: "apigeeapi"
  DEFAULT_APIGEE_ORG: "bap-emea-apigee-5"
  DEFAULT_APIGEE_ENV: "default-dev"
  TEST_HOST: "34.117.38.184.nip.io"
  APIGEE_ORG: "bap-emea-apigee-5"
  APIGEE_ENV: "default-dev"

steps:

- label: "Generate SA key file"
  commands: 
    - pwd
    - echo "$GCP_SERVICE_ACCOUNT_TOKEN" > ./sa.json
    - ls 
    - buildkite-agent artifact upload ./sa.json

- label: "Generate SA key file"
  commands: 
    - pwd
    - buildkite-agent artifact download sa.json .
    - ls
    # if Linux Agent 
    # - sed -i '' "s/target_apigee_env/$APIGEE_ENV/g" ./EdgeConfig/edge.json
    # if Mac Agent 
    - sed -i".bck" "s/target_apigee_env/$APIGEE_ENV/g" ./EdgeConfig/edge.json
    - cat ./EdgeConfig/edge.json
    - echo "___________________________________"
    - mvn -v
    - echo "___________________________________"
    - echo "$APIGEE_ORG" "$APIGEE_ENV"
    - echo "___________________________________"
    - mvn -X install -Pgoogleapi -Dapigee.org="bap-emea-apigee-5" -Denv="default-dev"  -Dapigee.config.file=EdgeConfig/edge.json -Dapigee.config.options=update